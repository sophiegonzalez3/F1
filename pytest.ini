[pytest]
testpaths = tests
; FutureWarnings fail the suite: the pandas<3 pin exists because of silent
; behaviour changes — this is the tripwire that catches the next one early.
filterwarnings =
    error::FutureWarning
