# NOTE: pandas is pinned <3 on purpose — the enrichment pipeline in
# processing.py relies on pandas 2.x groupby behaviour and raises
# KeyError on pandas 3.0. numpy is capped to its pandas-2.x-compatible line.
dash>=2.17,<3
dash-bootstrap-components>=1.6
plotly>=5.22
pandas>=2.2,<3
numpy>=1.26,<2.3
pyarrow>=16.0       # for Parquet caching
scipy>=1.13
livef1              # F1 data provider
fastf1>=3.8         # session fetch incl. X/Y position telemetry (racing-line analysis)
